** movelist.hpp

- Rendere class e non struct.
Uniformiamo la logica.

- Separare la logica in cpp e hpp
Vorrei entrare nel file hpp e vedere tutti
i metodi esposti in una volta sola senza
leggere i dettagli implementativi.

** piecevaluetables.hpp

- Rendere classe helper
- Creare metodi di accesso astratto:
es. getValueOfEndGamePawn(index, color)

Evitiamo di amministrare la logica del
mirroring degli indici dentro il codice.
Chi usa questa classe helper non ha idea
di come sia implementato il sottostante.

** clampToInt32 (funzione globale dentro engine.hpp)

- La stiamo usando in modo sensato?
Ho visto un paio di usi e non mi sembra
molto sensato come la stiamo usando.

Es.
engine/search/sorter.cpp:153:        return clampToInt32(score);

score qualche riga sopra è già int32.

** Enum GameResult

- Creare classe enum

** Engine.hpp
- Cambiare 'tt' con 'transpositionTable'.

** Evaluator

- Creare classe helper per ridurre
variabili passate.
Ci sono diversi sottosistemi dentro
la classe che lavorano in modo
indipendente.

Es. isWhitePassedPawn
classe helper con attributo maschera pedoni.
In questo caso passiamo da 3 attributi a 1!

Es. Tutti i metodi che prendono solo board.
Mettiamoli in una casse helper separata.

- Evitare funzione che ritorna void
per manipolare un parametro
passato.
Aumentiamo la chiarezza e diminuiamo
i parametri.

Es. ProcessPawn
oppure 
addKingCheckUnits
oppure
applyKingShieldSupport

** MoveGenerator

- Creare classe helper per racchiudere
occupancy e ownOccupancy.


** Searcher
- Astrarre logica alfa-beta.
API: es. hasToBeCut 

- Astrarre logica salvataggio informazioni.
Parlo di:
        chess::Board::Move killerMoves[2][MAX_PLY] {};
        int16_t history[2][64][64] {};
        uint16_t counterMoves[64][64] {};
        int16_t captureHistory[2][64][7][CAPTURE_HISTORY_SLOTS] {};

